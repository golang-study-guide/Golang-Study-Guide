package main

import (
  "fmt"
)


/*
In go there is only one loop construct, which is the 'for' loop. There is no 'foreach', 'while', or 'until' loops. 
However the for loop can be configured to:
loop until a condition is met (aka while/until-loop)
loop over a range - aka foreach loop
loop forevver

*/

func main() {
  fmt.Println("hello world")
}
